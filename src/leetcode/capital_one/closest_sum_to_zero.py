def closest_sum_to_zero(nums: list[int]) -> tuple[int, int]:
    """Given an array of integers, find two integers whose sum is closest to zero.
    Return the pair as a tuple in ascending order.

    If multiple pairs tie for closest, return any one of them.

    Example 1:
        Input: nums = [-3, -1, 1, 2]
        Output: (-1, 1)
        Explanation: -1 + 1 = 0, which is the closest to zero.

    Example 2:
        Input: nums = [-5, -4, -3, 3, 7]
        Output: (-3, 3)
        Explanation: -3 + 3 = 0.

    Example 3:
        Input: nums = [1, 2, 3]
        Output: (1, 2)
        Explanation: 1 + 2 = 3 is the closest to zero possible with positive-only input.

    Constraints:
        - 2 <= len(nums) <= 10^5
        - -10^9 <= nums[i] <= 10^9
        - All elements are distinct

    :param nums: a list of integers
    :return: a tuple of the two integers whose sum is closest to zero
    """
    pass
